let i = 0;

// el do se va ejecutar una vez y no importa si se cumple o no
//do while se va ejecutar una vez y despues comprueba
while (i < 50) {
    if (i % 2 === 0) {
        console.log(`el ${i} es par`)

    } else if (i % 5 === 0) {
        console.log(`el ${i} multiplo de 5`)
    }

}